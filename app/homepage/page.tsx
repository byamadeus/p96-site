// Legacy homepage — photo + dark overlay + nav links.
// Temporarily displaced by the World Cup landing page at /.
// Restore: move this back to app/page.tsx when the WC campaign ends.
import HomeClient from '@/components/Entry/HomeClient'

export default function Home() {
  return <HomeClient />
}
